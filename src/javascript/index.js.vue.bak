import Vue from 'vue'
import App from './App'

new Vue({
    el: '#app',
    components: { App },
    // template: '<App/>'
    render (h) {
      return h('App')
    }
  })
  //用template: '<App/>'写法需要编译环境。    换成函数写法。
  